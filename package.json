{
  "name": "chat-cats",
  "version": "0.1.0",
  "private": true,
  "description": "Cozy voxel cat overlay for Twitch/Kick chat",
  "scripts": {
    "dev": "live-server overlay --port=8080 --no-browser",
    "open": "live-server overlay --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
