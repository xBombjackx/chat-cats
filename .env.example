# copy to .env
TWITCH_CHANNEL=yourchannel
KICK_CHANNEL=yourchannel
# only needed if the kick chatroom lookup fails (cloudflare); find it in kick.com/api/v2/channels/<slug> -> chatroom.id
#KICK_CHATROOM_ID=
PORT=8080
COOLDOWN_MS=2500
MAX_CATS=30
RESPAWN_HOURS=6
